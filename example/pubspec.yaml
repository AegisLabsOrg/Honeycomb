name: honeycomb_example
description: Honeycomb state management demo app
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.7.0-0
  flutter: ">=3.27.0"

dependencies:
  flutter:
    sdk: flutter
  honeycomb:
    path: ../

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
